
❕ Fazer push:
git add .
git commit -m "Base para GitHub Pages"
git push origin main

❕ Commit vazio para forçar o workflow
git commit --allow-empty -m "Deploy limpo para GitHub Pages"
git push origin main
